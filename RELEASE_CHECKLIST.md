# 发版检查清单

每次发版照着走一遍。清单存在的意义是：**不依赖你当时记不记得。**
密码管理器的发版事故（发错签名、发了带 INTERNET 的包、校验和贴错）
一次就足够毁掉信任。

复制一份到 Issue 里逐项打勾也可以。

---

## 发版前

- [ ] `./gradlew test` 全绿
- [ ] `./gradlew assembleRelease` 通过
- [ ] `./gradlew lintRelease` 无 error 级问题
- [ ] 真机安装测试：**从旧版本升级安装**（不是全新安装），确认保险库能正常打开
- [ ] 真机测试解锁三条路：主密码 / PIN / 生物识别
- [ ] 真机测试自动填充：至少两个 App + 一个浏览器
- [ ] 加密备份 → 恢复 走通一遍
- [ ] `versionCode` 已 +1（**只增不减，永远**）
- [ ] `versionName` 已更新
- [ ] `CHANGELOG.md` 的「未发布」段落已整理成正式版本段落，并填了日期
- [ ] 如果 `.lvault` 格式有改动：CHANGELOG 里单列一节，说明向后兼容性
- [ ] 如果新增了依赖：`THIRD_PARTY_NOTICES.md` 已更新
- [ ] README 里的已知问题清单是否还准确

## 构建产物核对

- [ ] `apksigner verify --print-certs` 的 SHA-256 指纹与上一版**一致**
      （如果不一致，先停下来，见下方「指纹变更」）
- [ ] `aapt2 dump permissions` 只有 `USE_BIOMETRIC` 和 `POST_NOTIFICATIONS`
- [ ] APK 大小与上一版对比无异常增长（突然大一截通常意味着混进了不该有的依赖）
- [ ] `sha256sum LocalVault-x.y.z.apk > SHA256SUMS` 已生成
- [ ] 自己重新下载一遍产物并 `sha256sum -c SHA256SUMS` 验证通过

## 发布

- [ ] `git tag -a vX.Y.Z -m "X.Y.Z"` 并 push
- [ ] Release notes 已写，包含：
  - [ ] 变更摘要（面向用户的语言，不是 commit 列表）
  - [ ] 签名证书 SHA-256 指纹（每一版都重复贴，这是让用户养成核对习惯的唯一办法）
  - [ ] 校验命令示例
  - [ ] 已知问题
- [ ] APK 与 `SHA256SUMS` 已上传
- [ ] 勾选 "Set as the latest release"
- [ ] 从草稿改为正式发布

## 发布后

- [ ] 自己从 Release 页面下载一次，核对指纹和校验和（模拟用户视角）
- [ ] 关闭本版本修掉的 Issue，并留言说明在哪个版本修的
- [ ] 如果修的是安全问题：按 SECURITY.md 的承诺，在 Release notes 里描述问题、
      并致谢报告者（除非对方要求匿名）
- [ ] `CHANGELOG.md` 重新开一个空的「未发布」段落

---

## 指纹变更（希望永远用不上）

如果签名指纹与上一版不同，**在发布前停下来**：

1. 确认这是不是你自己有意为之（换密钥、上了 Play 应用签名）
2. 如果不是——你的构建环境可能已经被入侵，不要发布
3. 如果是——Release notes 的**最顶部**用醒目格式说明：
   - 旧指纹是什么，新指纹是什么
   - 为什么换
   - 用户需要卸载重装（签名不同无法覆盖升级），以及**卸载会丢数据，
     必须先做加密备份**

SECURITY.md 里对用户的承诺是「没有说明的指纹变更请当作可疑」。
这条承诺的另一半是：你有说明的义务，且说明必须显眼。
