# ─────────────────────────────────────────────────────────────
#  本地保险库 LocalVault — .gitignore
#
#  这个文件必须在第一次 commit 之前就位。
#  一旦某个文件被提交过一次，之后再加进这里也只是「以后不再跟踪」，
#  它仍然留在 git 历史里，任何人 clone 下来都能翻出来。
# ─────────────────────────────────────────────────────────────

# ── 本机配置 ──────────────────────────────────────────────────
# local.properties 里是 sdk.dir，含本机绝对路径和 Windows 用户名。
# 文件自己头一行就写着不该进版本控制。
local.properties

# ── 签名材料 ─────────────────────────────────────────────────
# 这几行是这份文件里最要紧的。签名密钥泄露 = 任何人都能用你的身份
# 发一个带后门的更新，而用户的系统会认为那是「同一个 App 的升级」，
# 静默覆盖安装。对密码管理器来说这是最坏的一种事故。
*.jks
*.keystore
*.p12
*.pepk
keystore.properties
signing.properties
key.properties

# ── Gradle ──────────────────────────────────────────────────
.gradle/
build/
# 注意：gradle/wrapper/gradle-wrapper.jar 要提交。
# 它是别人不装 Gradle 也能构建的前提，也是可复现构建的一部分。

# ── Kotlin ──────────────────────────────────────────────────
.kotlin/

# ── Android Studio / IntelliJ ───────────────────────────────
.idea/
*.iml
.cxx/
captures/

# ── 构建产物 ─────────────────────────────────────────────────
# APK 通过 Release 页面分发，不进仓库：二进制会让 clone 越来越重，
# 而且仓库里的 APK 没有校验和、没有构建来源，等于鼓励用户装来路不明的包。
*.apk
*.aab
*.ap_
*.dex
output-metadata.json

# ── 测试与分析产物 ───────────────────────────────────────────
*.exec
lint-results*.html
lint-results*.xml
/app/release/
/app/debug/

# ── 系统与编辑器垃圾 ─────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~

# ── 日志与崩溃转储 ───────────────────────────────────────────
*.log
hs_err_pid*
replay_pid*

# SSH 密钥。永远不要在工程目录里跑 ssh-keygen。
id_ed25519*
id_rsa*
*.pub
*.pem
